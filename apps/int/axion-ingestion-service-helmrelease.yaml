apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: axion-ingestion-service
  namespace: int
spec:
  interval: 5m
  chart:
    spec:
      chart: axion-ingestion-service
      version: ">=0.1.0"
      sourceRef:
        kind: HelmRepository
        name: acr-helm-repo
        namespace: flux-system
  values:
    environment: int
    replicaCount: 1
    image:
      repository: rakeshacr01.azurecr.io/int-axion-ingestion-service
      tag: "latest"
    ingress:
      enabled: false
      className: kong
      hosts:
        - host: int-ingestion.axion.local
          paths:
            - path: /
              pathType: Prefix
    env:
      DATABASE_URL: "postgresql://axion_user:Axiondb%401234@axion-db-service.int.svc.cluster.local:5432/axion_db"
